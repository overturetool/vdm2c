class ClassOp2

operations

op : () ==>()
op()==skip;

public affect : int*bool ==> bool
affect(x,z) == (
	if z then (
		if x>=4 and x<=10
		then op()
		elseif x = 13 then op()
		elseif x = 15 then op()
 	)
 	else (
		if x>=4 and x<=10
		then op()
		elseif x = 13 then op()
		elseif x = 15 then op()
 	);

 	return true;
)

end ClassOp2